#if !defined(FOOD_CHAIN_H)
#define FOOD_CHAIN_H
#include <vector>
#include <string>
#include <iostream>
using namespace std;
namespace food_chain {
    string verse(int n);
    string verses(int s, int e);
    string sing();
}  // namespace food_chain

#endif // FOOD_CHAIN_H