#if !defined(PRIME_FACTORS_H)
#define PRIME_FACTORS_H
#include <vector>

using namespace std;
namespace prime_factors {
    vector<int> of(int n);
}  // namespace prime_factors

#endif // PRIME_FACTORS_H